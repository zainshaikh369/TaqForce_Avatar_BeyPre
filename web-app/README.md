# Bey Avatar Web App

Minimal Next.js app to embed a Beyond Presence avatar via iframe. Enter an Agent ID on the page and click Embed.

Run locally:

1. Install node (v16+)
2. cd web-app
3. npm install
4. npm run dev

Open http://localhost:3000 and paste your agent id.
